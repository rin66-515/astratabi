# 《云月浮生》MVP 实装・测试记录

实施日：2026-08-17

对象分支：`main`

人工作品 Review：`未レビュー`

用户验收：`未実施`

## 1. 实装范围

| 项目 | 结果 |
|---|---|
| `/#fusheng` 独立路由 | `実装済` |
| 百工坊固定入口 | `実装済` |
| 首页木牌 12% 按日稳定入口 | `実装済` |
| 中日双语序章与八月 Event 00～09 | `実装済` |
| 状态、条件、flag、权重与负行动点 | `実装済` |
| 人物笺、月末账簿、九月预告 | `実装済` |
| localStorage 自动存档、继续、重开 | `実装済` |
| 移动端与减少动态效果 | `実装済` |
| 第二月正式内容 | `未実装` |
| 后台/跨设备存档 | `対象外` |

## 2. 自动检查结果

| 检查 | 命令 | 结果 |
|---|---|---|
| 引擎与木牌单体测试 | `npm.cmd test` | 2 files / 9 tests PASS |
| ESLint | `npm.cmd run lint` | PASS |
| TypeScript 与生产构建 | `npm.cmd run build` | PASS |
| 依赖安全审计 | `npm.cmd audit --omit=dev` | 0 vulnerabilities |
| 本地 HTTP | `http://127.0.0.1:18100/#fusheng` | HTTP 200 |
| 路由级分包 | Portal 主 JS / 游戏 JS | 251.82 kB / 161.43 kB，游戏按需加载 |

单体测试覆盖：

- 数值上限/下限与负行动点。
- 债务不得小于 0。
- stat、flag、completedEvent、month 条件。
- Event 08 的前序 flag 因果控制。
- 权重抽取的可控随机源。
- 游戏木牌 0%/100% 边界和同日稳定性。

## 3. ESLint 既存差异

项目原有 `src/App.tsx` 在引入 ESLint 前已存在退役预览组件、render 内随机值及旧式 effect 状态同步。为避免本次游戏制造扩大成后台/旧页面重构，兼容例外仅限定于 `src/App.tsx`；新游戏 feature 和其他 TypeScript 文件执行完整规则。

该例外不代表旧代码问题已解决。若后续拆分 `App.tsx`，应单独建立改善任务并逐项取消例外。

## 4. 未实施确认

- 未进行浏览器视觉 Review、不同移动设备实机确认和完整人工通关。
- 未确认浏览器刷新、关闭后继续、清除存档等人工操作。
- 未实施正式域名、Docker 镜像及服务器部署验证。
- 未实施第二月、30～45 个事件池和长期数值平衡。

以上项目不得记录为通过。下一步由用户在本地完成第一轮试玩，反馈文案、节奏、移动端尺寸和选择结果后，再形成指摘与修正记录。
